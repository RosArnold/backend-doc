# document-manage-platform(node-jwt)

A document manage platform with NodeJS, ExpressJS,MongoDB with jwt authentication

## Install

`npm install`

## Run

`npm start`

## Roadmap

- Improve error handling.
- Add CI support.
- Code quality
  - Use `async` `await`.

## DB Seeder

`node seeder.js`
